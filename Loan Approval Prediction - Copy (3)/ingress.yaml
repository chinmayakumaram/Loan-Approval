apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loan-approval-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: loan-approval.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: loan-approval-service
                port: